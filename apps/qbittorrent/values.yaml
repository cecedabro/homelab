env:
  TZ: "Europe/Amsterdam"
  WEBUI_PORT: "8080"

service:
  main:
    ports:
      http:
        port: 8080

persistence:
  config:
    enabled: true
    storageClass: longhorn
    size: 1Gi
  downloads:
    enabled: true
    existingClaim: nfs-media
    mountPath: /media